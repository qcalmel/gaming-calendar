import { Typography } from "@mui/material";

const Calendar = () => {
  return (
    <Typography sx={{ textAlign: "center" }} variant="h1">
      Calendar
    </Typography>
  );
};

export default Calendar;
